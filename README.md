My custom configs for a co-op Insurgency: Sandstorm server. 

Includes a Python script that fixes the map-vote freeze (when no one votes at the end of a match) and automatically shows "Player connected/disconnected" messages in the game chat.
